#What  are methods of set?
# The methods of the set data type in Python include:
# add(element)
# clear()
# copy()
# difference(set)
# difference_update(set)
# discard(element)
# intersection(set)
# intersection_update(set)
# isdisjoint(set)
# issubset(set)
# issuperset(set)
# pop()
# remove(element)
# symmetric_difference(set)
# symmetric_difference_update(set)
# union(set)
# update(set)

#2)What is tuple
#  A tuple in Python is an immutable, ordered collection of elements. It is defined by enclosing a
#  sequence of comma-separated values within parentheses, like this: (value1, value2, value3). Tuples
#  are commonly used to store multiple items of heterogeneous data types, such as integers, strings,
#  or other objects.

